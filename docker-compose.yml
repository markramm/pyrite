services:
  pyrite:
    build: .
    ports:
      - "8088:8088"
    volumes:
      - pyrite-data:/data
    environment:
      - PYRITE_HOST=0.0.0.0
    restart: unless-stopped

volumes:
  pyrite-data:
