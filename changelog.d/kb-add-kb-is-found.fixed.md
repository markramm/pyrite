- **`pyrite search -k`, `kb validate`, `kb schema show|add-type|remove-type|set` and `schema validate -k` now find a KB added with `pyrite kb add` (#363).** They read `config.yaml` alone and answered `KB_NOT_FOUND` for a KB that `get` and `index sync` accept; they now build their config through the shared helpers in `pyrite/cli/context.py`, which merge the database registry. A bare `kb validate` covers `kb add` KBs too.
