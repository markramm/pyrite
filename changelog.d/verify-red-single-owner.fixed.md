- `scripts/verify-red.sh` and the CI `verify-red` job now share one owner for reverting and restoring the implementation files: the files are written directly and put back byte for byte however the run ends (Ctrl-C, SIGTERM, a killed or timed-out test run), the git index is never touched, an edit made during the run is never overwritten, and a file that cannot be restored is named with a non-zero exit. Checkout conversion (`eol=crlf`) and non-UTF-8 files restore correctly; `unittest.mock` patch misses and `monkeypatch.delattr` count as weak reds, and text quoted in an assertion message no longer does; a node id pytest cannot find is "no claim" instead of "fails without the fix" (#368).
