---
id: groom-2026-09-18-tick6
title: "Groom lane breakdown — 2026-09-18 tick 6 (0.24.2 remaining, pool, quality stock)"
type: note
tags: [conductor, groom, architect, 0.24.2]
created: "2026-09-18"
---

The `pyrite-architect`'s build breakdown for ticks 6–8, dispatched at tick 6 (09:15Z) over milestone 0.24.2, the roadmap's definition of done, the pull-forward pool and the quality stock. Recorded on the log branch in the same tick per #141 (a record that is not pushed is not a record). Baseline `origin/dev` 4e34b80. Already dispatched at tick 6 and excluded here: Playwright A.1 (#138), pin-the-runner (#139), `scripts/release.py` (#140).

## Themes (in dispatch order)

### 6A — Filters are honoured on every search leg (#56) — opus — closes #56
Acceptance: a test per filter × mode (keyword, semantic, hybrid) with a bogus value returning 0 and a valid value returning only matching entries, on both backends; REST `/api/search` checked for the same leg split. Preference order from the ticket: (1) filters on the vector leg; (2) post-filter the fused set; (3) at minimum a `warnings: []` field. The worker must pick 1 or 2 explicitly and say which; never 3 alone.
Touches: `pyrite/services/search_service.py` (`_semantic_search` ~:336, `_hybrid_search` ~:361 — the vector leg is called with `kb_name` only; `status` alone is post-filtered ~:410, the shape to generalize), `pyrite/storage/backends/protocol.py` (:128 `search_semantic`), `sqlite_backend.py` (:314), `postgres_backend.py` (:320), `overlay_backend.py` (:327), `pyrite/server/endpoints/search.py`, `tests/backends/test_backend_conformance.py`; new `tests/test_search_filters_across_modes.py`.
Sequence: independent; never concurrent with another search_service theme. Cold read: yes (a 13-method `SearchBackend` protocol changes shape; storage). heavy: no.
Out of scope: #59 (vocabulary drift), #62 (`kb_timeline` `kb_name`), the "unknown entry_type → VALIDATION error" idea (new public error contract).

### 6B — Playwright package F: graph + timeline specs — sonnet — heavy
Acceptance: the ticket's B–G block (assert on seeded data; roles/`href`s/`data-testid` not text; `data-testid` in the component only where no role exists; 5× green; zero `text=`; no `.first()` dodges).
Touches: `web/e2e/graph.spec.ts` (6 `text=` incl. `text=Knowledge Graph` and the `\d+ nodes, \d+ edges` regex; two `.first()` hops that reach outside the package — rewrite onto seeded ids), `web/e2e/timeline.spec.ts` (`text=/\d+ events/`, `text=No timeline events found`); only if no role exists: `web/src/routes/graph/+page.svelte`, `web/src/lib/components/GraphView.svelte`, `web/src/routes/timeline/+page.svelte`.
Sequence: after A.1 (#118/#138) lands; parallel with G. Cold read: no.
Out of scope: package A's four files (report a missing seed, do not edit it); the `e2e` CI job (H); #117; #49 (skip a `toHaveTitle` with a comment naming #49).

### 6C — Playwright package G: qa + search specs — sonnet — heavy
Acceptance: as F. Touches: `web/e2e/qa.spec.ts` (`text=QA Dashboard`, `text=Loading QA data...`, `text=Total Entries`, `text=No issues found.`), `web/e2e/search.spec.ts` (`text=No results found`, `a[href^="/entries/"].first()`); only if no role exists: `web/src/routes/qa/+page.svelte`, `web/src/routes/search/+page.svelte`.
Sequence: after A.1; parallel with F. Cold read: no.
Out of scope: root-causing #9 — G asserts rendered result text against the seeded world (criterion 2 of #9) but must not touch `web/src/lib/stores/search.svelte.ts`. Footprint warning: G and the #9 fix both touch `web/src/routes/search/+page.svelte` — dispatch G first, rebase #9 onto it, never parallel.

### 6D — Round-trip identity gate (quality) — sonnet — closes `no-op-round-trip-is-byte-identical…`
Acceptance verbatim from the item (passes on `dev` after #69 or xfails a named shrinking list; reintroducing #46/#86/#87 makes it fail; < 10 s; the adversarial fixture set under `tests/fixtures/roundtrip/`).
Touches: new `tests/test_roundtrip_identity.py`, `tests/fixtures/roundtrip/`; possibly `pyrite/models/base.py` for the `links:` residual (148 of 766 files on #69's branch).
Sequence: after #69 merges (hard). Cold read: no (yes → opus if the worker takes the residual fix). heavy: no.
What the spec must include, given #69's final design: `Entry.__setattr__` (~:292) maintains `_absent_default_keys` — a key equal to its default and absent from the file stays absent on save unless something assigns it (~:309), so the test must load through the repository and save without touching any attribute (an incidental `setattr`, e.g. a helper normalizing `updated_at`, makes files gain keys for a non-bug reason). `_pristine_frontmatter(cls)` (~:85) is a cached per-class probe — shared state across the whole walk; require a test that walks two entries of one type with different absent-key sets and asserts no contamination. `_omit_default`/`_always_written_keys` (~:104) and `_restyle_like_source` (~:191) are the mechanisms; the residual is `_keep_sequence_style` (~:170) not covering bare-string→mapping in `links:`. If not fixed, the xfail list names exact ids and the count is recorded. Run against a temp copy of the real `kb/` (766 entries); never save in place.
Out of scope: #87 and #90 (the link write path and the links restyle) — the gate, not the fix.

### 6E — Packaged web UI: the install one-liner serves a working SPA — opus — closes `installable-from-github-with-a-working-web-ui-package-the-built-frontend`
Acceptance verbatim: in a clean venv the one-liner installs and `pyrite-server` serves the SPA at `/` with fonts and icons (a `.woff2` and an `.svg` return 200); no static dir → one clear warning saying how to get a UI; the release runbook's clean-venv step checks the UI; README Quick Start leads with the `uv tool install` one-liner.
Touches: `.gitignore` (bare `dist/` matches at any depth — needs a negation), `MANIFEST.in` (`recursive-include pyrite/static *`), `pyproject.toml` (`[tool.setuptools.package-data]`), the static-mount resolution in `pyrite/server/api.py` (today `Path(__file__).parent.parent.parent / "web" / "dist"`, silently false in site-packages), `README.md`, `release-runbook.md`; new `pyrite/static/` (the committed build) and a test that the committed build matches the frontend source hash recorded at build time.
Sequence: independent, but coordinates with `scripts/release.py` (#140) on the runbook's clean-venv step — release.py first, this rebases onto it, or this theme owns the runbook edit and the release.py worker leaves it alone (say which in the spec). Cold read: yes (packaging shape + static-mount path; a wrong negation or glob ships an empty UI silently). heavy: no.
Out of scope: step 2 (release-attached wheel, deferred to "when next touching CI"); the PyPI name; frontend source changes.

### 6F — CI parity: extensions linted, fix-needs-a-test enforced on PRs — sonnet — closes `ci-parity-lint-extensions-and-enforce-the-fix-needs-a-test-rule`
Acceptance verbatim: `ruff check pyrite/ tests/ extensions/` clean and enforced in CI; a `fix:` commit without a `tests/` change fails CI on a PR; `tests/test_dev_process_config.py` pins both.
Touches: `extensions/**` (mechanical `ruff check --fix` + `format` — ~60 errors, ~55 files), `.github/workflows/ci.yml` (ruff step + a step running `scripts/check_fix_commit_has_tests.py` over the PR's range), `tests/test_dev_process_config.py`.
Sequence: only when nothing touching `extensions/` is in flight (clear at tick 6); not with the software-kb split. Cold read: no. heavy: no.
Out of scope: mypy; the `e2e` job (H); any behaviour change inside the reformat — the diff must be `--fix`/`format` output plus the CI lines, stated in the report.

### 7A — Playwright package H: the e2e job blocks on `main` — sonnet — closes the Playwright ticket
Acceptance verbatim (H): `e2e` runs on pushes to `main` with `retries: 0`; the report artifact stays; `continue-on-error` goes; `tests/test_dev_process_config.py` pinned; on `dev` only after ten consecutive greens on `main`. Plus the ticket's own: "`continue-on-error: true` is removed from the `e2e` job in `ci.yml` and a full CI run passes with e2e blocking again."
Touches: `.github/workflows/ci.yml` (the `e2e` job ~:265 — `workflow_dispatch` condition becomes push-to-`main`; `continue-on-error` ~:290 and its REVERT comment go), `tests/test_dev_process_config.py`, `web/playwright.config.ts` only if `retries` ≠ 0, the Playwright ticket (close).
Sequence: after F and G merge and after A.1; never the same tick as F/G. Cold read: yes (it changes the gate; a wrong `if:` runs e2e on every PR or on nothing, invisibly). heavy: no for the diff.
Couplings with `scripts/release.py`: (1) `e2e` must not enter `gate`'s `needs` — adding it would make release.py's CI wait block on a 7-minute browser suite before every tag; (2) release.py's CI-wait must name which checks it waits on; `e2e` is advisory on `main`, not a release gate — tell the release.py worker now, not at the tag. Evidence gap: H cannot prove "passes on `main`" from a branch — require green `workflow_dispatch` runs on the branch, and the conductor watches the first real `main` run after merge.
Out of scope: flipping the `dev` trigger (ten greens first); spec rewrites; package A's world.

### 7B — Index sync converges and health tells the truth — sonnet — closes #6 #7 #8 #22 #47 #44
Acceptance (verbatim, merged): #6 sync warns loudly when two paths map to one id; `index health` gets a `duplicate_ids` check (id → [paths]); two consecutive syncs with no changes report `Updated: 0`. #7 sync compares the stored content hash rather than mtime alone, or acts on whatever `health` reports as `content_changed` so the two agree. #8 `missing_files` means absent from the filesystem, nothing else; unparseable files reported once under `malformed_frontmatter`. #22 `kb.yaml` gets an `exclude:` list of gitignore-style globs honoured by `index build`/`sync`/`health`; excluded paths appear in no check and are not `unindexed_files`. #47 a test with an on-disk off-enum value; `index health` names it; updating an unrelated field succeeds or the error says how to repair. #44 normalize both sides (`declared_sub.strip('/')`), presets made consistent; tighten the assertion in `scripts/run_tutorial.py`.
Touches: `pyrite/storage/index.py` (`check_health`, 231 lines), `pyrite/cli/index_commands.py`, `pyrite/config.py` (the `exclude:` loader), the type presets writing `subdirectory: people/`, `scripts/run_tutorial.py`; new `tests/test_index_sync_convergence.py`, `tests/test_index_health_classification.py`.
Sequence: after #69 merges (hard collision on `index.py`/`index_commands.py`); independent of #19 (reserved). Cold read: yes (storage; `exclude:` is a new public config key). heavy: no.
Split advice: six issues is the top of the reviewer test — cut #22 (the only new public surface) into a second theme; dispatch #6/#7/#8/#44/#47 first.
Out of scope: the `check_health` split; #51 (a cascade-KB data-migration question); #59.

### 7C — Page titles survive the layout (#49) — sonnet — closes #49
Acceptance: the root layout's `$effect` sets `document.title` from `brandStore.name` once `brandStore.loaded` flips, clobbering every route's `<svelte:head><title>`; SSR off. A route's own title survives; the brand name is the fallback only where a route declares none.
Touches: `web/src/routes/+layout.svelte`; new unit test under `web/src/` or a `toHaveTitle` in an existing spec.
Sequence: after F/G if it lands a `toHaveTitle` in their specs; independent otherwise. Cold read: no. heavy: no (yes if it takes a Playwright assertion).
Pairs naturally with #45 ("the web UI tells the truth while it loads") — #45 touches `web/src/routes/entries/+page.svelte` and `EntryList.svelte`, which D already rewrote specs against; either as one theme, say so in the spec.

### 7D — MCP read-tier ergonomics, the unreserved half — sonnet — closes #62 #66
Acceptance: #66 tests for the error code + suggestion and for `detail='brief'` omitting write-side keys (the three findings: `kb_orient` cannot be first, `OPERATION_FAILED` vs the contract's `KB_NOT_FOUND`, ~9,000 tokens for ~1,200 used). #62 expose `status`/`exclude_status` and `kb_names` on `kb_timeline`.
Touches: `pyrite/server/mcp_server.py` (`_kb_timeline` ~:404, `_kb_orient` ~:592), `pyrite/server/tool_schemas.py` (`kb_timeline` ~:115, `kb_orient` ~:209); new `tests/test_mcp_read_tier_ergonomics.py`.
Sequence: independent; not co-dispatched with 6A if 6A takes option 3 (also edits `tool_schemas.py`). Cold read: yes (MCP tool arguments are a public shape). heavy: no.
Reserved for outside contributors until 2026-09-19 08:45Z — a worker leaves these alone: #58, #65, #67, #68, #95, #96, #44, #12, #19, #20, #21, #47, #63, #134, #137. Corrections: #64 is NOT labelled (groomable) but overlaps #124 (`allow_dangling` + `resolved`, the successor to #116/#108) — treat #64+#124 as one future theme. #44 and #47 ARE labelled and reserved — collides with 7B (see decisions).
Out of scope: everything reserved; `kb_orient`'s payload beyond adding `detail` (delete nothing from `full`).

### 7E — Batch-read contract parity, REST and MCP — sonnet — closes #134 #137
Blocked: both `good first issue`, reserved until 2026-09-19 08:45Z; #137 offered to @makiaveli1. Dispatch at tick 8 if unclaimed.
Acceptance: #137 — validate each spec has `entry_id` and `kb_name`, `VALIDATION_FAILED` with `retryable: false`, a malformed-spec test; a CHANGELOG line for #126; narrow the schema sentence to `kb_batch_read` or make the identity pair always-included across all `fields` projections (one helper) with a test per tool. #134 — identity pair always projected on REST `/entries/batch`, structured `VALIDATION` error, a `fields` test asserting `found`/`not_found` consistent.
Touches: `pyrite/server/mcp_server.py`, `tool_schemas.py`, `pyrite/server/endpoints/entries.py` (:356, :363, :365), `CHANGELOG.md`; tests beside the existing batch-read tests.
Cold read: yes if "always-included across all tools"; no for the narrow option. heavy: no.
Out of scope: #58 (reserved, same function); routing both through one service-level batch read (`mcp-rest-tool-parity` owns it — say no to the temptation).

## Needs a spike first
- **#9 — web search results never render.** Root cause unknown and previously un-reproducible: the ticket's 2026-07-04 trace ruled out the obvious (one `{#if}` chain reads one store; `execute()` clears `loading` in `finally`; the two 401s are `authStore.init()` + `Sidebar.svelte` both calling `/auth/me`, independently guarded; no fetch wrapper, no `hooks.client.ts`); three live repros against demo.pyrite.wiki rendered correctly. Residual hypothesis: a stale cached JS bundle mismatched with the API shape — invisible to a source trace. Spike (explorer, or a worker with `pyrite-explorer`): a fresh browser repro with the two exact 401 request URLs and the served bundle hash, or close #9 and convert its acceptance into package G's assertion. No fix theme in tick 6. Footprint if it becomes a fix: `web/src/lib/stores/search.svelte.ts` (61 lines), `web/src/routes/search/+page.svelte` (435) — after G either way.
- **#13 — first write blocks on the model download.** Root cause known (`KBService._auto_embed` at `kb_service.py:140` embeds synchronously when `self._embedding_worker is None`; the download and the torch import happen inside the request) — not a root-cause spike but a design choice the ticket leaves open (background thread vs skip-and-let-sync-catch-up). Interacts with: #43 (`pyrite init` writes no `auto_embed` key, so a fresh tutorial KB embeds nothing on write and semantic search returns 0 — the same question from the CLI side; one theme for both, opus); PR #35's `settings.auto_embed` (`config.py:350`, default True; `kb_service.py:87`; `tests/test_auto_embed_setting.py`) is the existing switch — no second one; `embedding_worker.py` (139 lines) exists and `_auto_embed` enqueues when set — the real question is who sets `_embedding_worker` on the server path, and whether unset means background or skip; #102 (`submit_sync` starts an unjoined daemon thread) warns against a naive thread, #127 is the test-side cost. Acceptance verbatim (#13): "With an empty HF cache and the network blocked, `POST /api/entries` returns in < 2 s and the entry is keyword-searchable (live-server test). The README says the first semantic search downloads a model." Footprint: `kb_service.py`, `embedding_service.py`, `embedding_worker.py`, `server/api.py` (startup wiring — PR #5's prewarm), `config.py` + the `init` template for #43, `README.md`; new `tests/e2e/test_fresh_install_write.py`. Cold read: yes. heavy: yes (live server; HF cache empty and network blocked). Independent of #69; not with anything touching `kb_service.py`.

## Needs a decision first
- **#59 (entry_type drift, 187 values)** — is `entry_type` a controlled vocabulary or free text, and if controlled, normalize the data or alias on read? Three different answers in the ticket with different consequences for every KB on disk → an ADR (with ADR-0029 or new): does a `type_aliases` read-time map make the index's `entry_type` column non-authoritative, and what does `kb_stats` report? Prerequisite for #56 being *useful*, not for it being *correct* — do not block 6A.
- **The reserved-label collision on #44 and #47** (both in 7B). Options: (a) drop them from 7B — but #44 is a two-character fix inside the exact `check_health` the theme rewrites; (b) un-reserve #44; (c) hold 7B until the window expires — it is blocked on #69 anyway. Recommend (c); decide at tick 7.
- **Package H's e2e-on-`main` vs `scripts/release.py`'s CI wait** — settle between the two workers, not at the tag (detail under 7A).

## Not now
- **README reposition** — sequenced "after the security fixes, the MCP tool fixes and the contributor docs pass" (the door landed as #136); depends on `docs-counts-generated-or-asserted-from-code` for its facts. Counts first, reposition after; hold for tick 7–8, and consider whether the maintainer's own voice is the deliverable.
- **`docs-counts-generated-or-asserted-from-code`** — ready (new `tests/test_docs_facts.py`; `README.md`, `kb/positioning/README.md`, `UPSTREAM_CHANGES.md`, `CLAUDE.md`; sonnet). Overlaps `single-source-of-truth-for-the-version-asserted-by-a-test` — merge them into one theme at tick 7, scoped to the `[Unreleased]` discipline + docs facts; `web/package.json` and `pyrite-mcp` versions wait on ADR-0031.
- **`changelog-fragments…`** — ready (sonnet; `CHANGELOG.md`, new `changelog.d/`, `scripts/release.py`, `tests/test_dev_process_config.py`, the two skills); #103 says CHANGELOG is the one file every theme touches and a conflicted PR gets zero checks. Footprint is `scripts/release.py`, dispatched this tick — fold into release.py, or dispatch at tick 7 right after it merges. The highest-leverage process item in the pool.
- **KB registry single source of truth** — item 1 (the `all_kbs()` sweep) was already fixed 2026-07-03; what remains (one library YAML, `kb create`/`kb add` write it, the DB `kb` table as a derived cache, load-time validation) is what ADR-0029 phases and a migration of every user's `~/.pyrite/config.yaml` — release-shaped, not a pull-forward; collides with #19 (reserved). Re-read ADR-0029's phasing with the maintainer first.
- **God-object splits** — all `priority: low`, "do each when next working in that file"; every one collides with a theme here (`mcp_server.py` ↔ 7D/7E; `endpoints/entries.py` ↔ 7E; `storage/index.py` ↔ 7B; `kb_service.py` ↔ #13; software-kb `plugin.py` ↔ 6F's sweep). Do not dispatch a split while the bug themes are in flight; schedule `split-entries-endpoint` (802 LOC, smallest) for the first tick where nothing touches `endpoints/`.
- **Worker findings:** #45 ready (pairs with #49 as 7C). #48 ready, sonnet, `extensions/cascade/…` + `pyrite/schema/kb_schema.py:391,400` — after 6F's sweep. #51 a data-migration question on a live cascade KB; needs the #69/#15 known-keys story settled; hold. #52 ready, tiny, `pyrite/cli/task_commands.py` — filler. #54 a decision: a lean default search shape changes the CLI and MCP response for every caller; `filters_dropped` is also #56's option 3 — settle inside 6A's report ("what a search response owes its caller"), then file the rest as small tickets. #117 a UI feature gap — "Not in 0.24.2: new UI surface"; 0.25 unless wanted for the pilot. #135 ready, sonnet, well-measured, `pyrite/storage/repository.py:260`, cold read yes (the lookup was just changed by #108's fixup, `6d97774`) — tick 7. #127 — PR #81 may have fixed it; re-run `pytest tests/test_api_tiers.py tests/test_index_worker.py -n auto` three times and close rather than dispatch. #43 folded into #13.
- **#10/#11/#12 (web first-visit)** — #12 reserved; #11 blocked on #10's design ("do not invent a fourth scoping regime"); #10 is ADR-shaped (URL-first, switcher navigates). A design note or ADR from the maintainer, then #10+#11 as one Opus theme in 0.25 where the roadmap places them.
